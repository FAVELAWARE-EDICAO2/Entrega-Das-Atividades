function verificarSenha(senha) {
    if (senha.length >= 8) {
        console.log("Senha válida");
    } else {
        console.log("A senha deve ter pelo menos 8 caracteres");
    }
}

var senha = "exemplo123";
verificarSenha('otorrinolaringolojista'); 